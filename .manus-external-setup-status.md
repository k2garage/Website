# External setup status — 2026-09-19

- Resend domain `k2garage.cz` is verified for sending.
- A new Resend API key named `K2 garage — Vercel reservation form` was created with `Sending access`, restricted to the `k2garage.cz` domain. The key value is retained only for immediate secure insertion into Vercel and must not be committed or documented.
- The GitHub repository `k2garage/Website` has been imported in the Vercel browser flow under the `K2GARAGE` Hobby team. Vercel detected the Vite framework, with root directory `./`.
- The project has not yet been deployed; project name is currently `website` and can be renamed before the first deploy.
- The next steps are: name/deploy the project, create a private Vercel Blob Store linked to the project, add `RESEND_API_KEY` and `BLOB_READ_WRITE_TOKEN` as encrypted Vercel environment variables, then deploy a server implementation for `/api/reservation`.

Vercel import bude vytvořen ve stejném Hobby týmu pod názvem projektu `k2-garage`; framework je Vite a kořenový adresář je `./`.

Vercel projekt `k2-garage` byl vytvořen z repozitáře `k2garage/Website` a první nasazení úspěšně dokončeno na dočasné Vercel adrese.

Soukromý Blob Store `k2-garage-reservation-photos` je propojen s projektem `k2-garage`; Vercel vytvořil `BLOB_READ_WRITE_TOKEN`, `BLOB_STORE_ID` a veřejný webhook klíč pro Production, Preview a Development. Tajná proměnná `RESEND_API_KEY` je uložená ve Vercelu, ale je potřeba rozšířit ji z výchozího Production prostředí také na Preview a Development.

`RESEND_API_KEY` nyní cílí na všechna prostředí Vercelu: Production, Preview i Development. Čeká pouze na uložení této změny a nové nasazení, aby se proměnná propsala do běžící aplikace.
